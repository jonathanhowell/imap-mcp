---
phase: 12-connection-resilience-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/connections/error-classifier.test.ts
  - tests/connections/account-connection.test.ts
  - tests/connections/connection-manager.test.ts
  - tests/polling/poller.test.ts
  - tests/startup.test.ts
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - CONN-04
  - CONN-05
  - CONN-06
  - CONN-07
tags:
  - imap
  - connections
  - tests
  - wave-0

must_haves:
  truths:
    - "A failing test exists for every CONN-01..CONN-07 behavior listed in 12-VALIDATION.md before any implementation lands"
    - "Test file tests/connections/error-classifier.test.ts exists and `npm test` reports it red (module under test does not exist yet)"
    - "Existing tests/connections/account-connection.test.ts has new red tests with the exact `-t` names recorded in 12-VALIDATION.md"
    - "Existing tests/connections/connection-manager.test.ts compiles after the suspended-case test stub is added (test fails because consumers still use `failed`)"
    - "tests/polling/poller.test.ts has skip-on-non-connected red tests"
    - "tests/startup.test.ts has an `unhandledRejection logs and continues` red test"
  artifacts:
    - path: "tests/connections/error-classifier.test.ts"
      provides: "Wave 0 scaffold for CONN-01"
      contains: 'describe("classifyConnectionError"'
    - path: "tests/connections/account-connection.test.ts"
      provides: "Extended Wave 0 scaffolds for CONN-02..CONN-06"
      contains: 'unbounded transient retry'
    - path: "tests/polling/poller.test.ts"
      provides: "Wave 0 scaffolds for CONN-07"
      contains: 'skips non-connected accounts'
    - path: "tests/startup.test.ts"
      provides: "Cross-cutting Wave 0 scaffold for the unhandledRejection handler"
      contains: 'unhandledRejection logs and continues'
  key_links:
    - from: "tests/connections/error-classifier.test.ts"
      to: "src/connections/error-classifier.ts (to be created by Plan 02)"
      via: 'import { classifyConnectionError } from "../../src/connections/error-classifier.js"'
      pattern: 'from "../../src/connections/error-classifier.js"'
    - from: "tests/connections/account-connection.test.ts"
      to: "src/connections/account-connection.ts (refactored by Plan 03)"
      via: "existing test imports — new test bodies assert on new behavior"
      pattern: 'AccountConnection|reconnectInFlight|suspended'
---

<objective>
Create failing test scaffolds for every CONN-01..CONN-07 behavior recorded in 12-VALIDATION.md so that subsequent waves can drive their implementations green. No production code is modified in this wave — only tests.

Purpose: Wave 0 establishes the Nyquist test boundary for Phase 12. Every requirement gets a red test before implementation; implementers in Waves 1–3 drive each red test green.
Output: Five test files (one new, four extended) covering CONN-01 through CONN-07 plus the cross-cutting `unhandledRejection` behavior.
</objective>

<execution_context>
@/Users/jonathanhowell/code/imap-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanhowell/code/imap-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-connection-resilience-foundation/12-CONTEXT.md
@.planning/phases/12-connection-resilience-foundation/12-RESEARCH.md
@.planning/phases/12-connection-resilience-foundation/12-VALIDATION.md

<interfaces>
<!-- Test imports — these symbols exist (in their current shape) or will exist after Wave 1-3. -->

From src/connections/account-connection.ts (current shape — Wave 2 will refactor):
```typescript
export type AccountConnectionStatus =
  | { kind: "connecting" }
  | { kind: "connected"; client: ImapFlow }
  | { kind: "reconnecting"; attempt: number; nextRetryAt: Date }
  | { kind: "failed"; reason: string }; // dropped in Wave 2

export class AccountConnection {
  constructor(accountId: string, config: AccountConfig);
  getStatus(): AccountConnectionStatus;
  async connect(): Promise<void>;
  async gracefulClose(): Promise<void>;
}
```

From src/connections/error-classifier.ts (DOES NOT EXIST YET — Plan 02 creates it):
```typescript
export type ErrorClass = "transient" | "fatal";
export function classifyConnectionError(err: unknown): ErrorClass;
export function humanReason(err: unknown): string;
```

From imapflow (existing dependency, version bumped in Plan 04 to ^1.3.7):
```typescript
export class AuthenticationFailure extends Error { /* ... */ }
```

Existing test patterns (reuse, do not reinvent):
- `vi.mock("imapflow", () => ({ ImapFlow: vi.fn(function() { return makeMockClient(); }) }))`
- `makeMockClient()` helper returning EventEmitter + connect/logout/close stubs
- `flushMicrotasks(times = 5)` helper
- `vi.useFakeTimers()` in `beforeEach`; `vi.advanceTimersByTimeAsync(ms)` to drive backoff
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/connections/error-classifier.test.ts with red CONN-01 scaffolds</name>
  <files>tests/connections/error-classifier.test.ts</files>
  <read_first>
    - .planning/phases/12-connection-resilience-foundation/12-VALIDATION.md (the test contract — `-t` names below MUST match the table verbatim)
    - .planning/phases/12-connection-resilience-foundation/12-CONTEXT.md (D-05 fatal list, D-06 transient list)
    - .planning/phases/12-connection-resilience-foundation/12-RESEARCH.md §"Pattern 1: Error Classifier" (sample shape)
    - tests/connections/account-connection.test.ts (existing test style — imports, `vi.mock("imapflow", ...)`, `describe`/`it` patterns)
    - tests/connections/connection-manager.test.ts (more existing test style reference)
  </read_first>
  <action>
Create NEW file `tests/connections/error-classifier.test.ts` importing the not-yet-existent `classifyConnectionError` and `humanReason` from `../../src/connections/error-classifier.js`. The test file must compile (the import resolves at test-runtime via Vitest's ts-jit; the missing source surfaces as red tests at run time, not as a TypeScript error — Plan 02 creates the file).

File content structure (write it exactly along these lines):

1. Imports:
   - `import { describe, it, expect } from "vitest";`
   - `import { classifyConnectionError, humanReason } from "../../src/connections/error-classifier.js";`
   - `import { AuthenticationFailure } from "imapflow";`

2. Three `describe` blocks with the EXACT `-t` names recorded in 12-VALIDATION.md (the verifier runs `vitest -t "..."`):

   a. `describe("classifyConnectionError", () => {` containing:
      - `it("classifies fatal sources: AuthenticationFailure instance", ...)` — construct an `AuthenticationFailure` (`new AuthenticationFailure("auth failed")` — if the constructor signature differs, fall back to `Object.setPrototypeOf(new Error("auth"), AuthenticationFailure.prototype)`); assert `classifyConnectionError(err) === "fatal"`
      - `it("classifies fatal sources: tlsFailed flag", ...)` — `const err = Object.assign(new Error("tls"), { tlsFailed: true })`; assert returns `"fatal"`
      - One `it.each` (or repeated `it`) per RFC 5530 fatal code from D-05: `AUTHENTICATIONFAILED`, `LOGINDISABLED`, `PRIVACYREQUIRED`, `OVERQUOTA`, `UNAVAILABLE`, `EXPIRED`, `ALERT`, `CONTACTADMIN`. Each constructs `Object.assign(new Error("rfc5530"), { serverResponseCode: <code> })`. Test title must include the phrase `classifies fatal sources` so the validator `-t "classifies fatal sources"` matches.
      - Combined-title alternative if `it.each` is used: a single block titled `classifies fatal sources (RFC 5530 + AuthenticationFailure + tlsFailed)` is acceptable as long as the validator `-t "classifies fatal sources"` matches.

   b. `describe("classifyConnectionError", () => {` (or extend the above) containing `it("classifies transient sources", ...)` driving the codes from D-06: `ECONNRESET`, `ETIMEDOUT`, `ENOTFOUND`, `ECONNREFUSED`, `ENETUNREACH`, `EConnectionClosed`, `NoConnection`. Each test constructs `Object.assign(new Error("net"), { code: <CODE> })` (or sets `.code` per the project's typical Node error shape) and asserts `classifyConnectionError(err) === "transient"`. Title must include phrase `classifies transient sources`.

   c. `it("defaults unknown to transient", () => { expect(classifyConnectionError({ weirdShape: true })).toBe("transient"); expect(classifyConnectionError(null)).toBe("transient"); expect(classifyConnectionError(undefined)).toBe("transient"); expect(classifyConnectionError(new Error("totally unknown thing"))).toBe("transient"); });`

3. A `describe("humanReason", () => {` block with one smoke test: `it("returns a non-empty string for known fatal sources", () => { const auth = Object.assign(new Error("ignore me"), { serverResponseCode: "AUTHENTICATIONFAILED" }); expect(humanReason(auth)).toMatch(/authentication/i); });` (Plan 02 will lock the exact strings per D-02 and Open Question 3 in RESEARCH.md; this scaffold just asserts the helper exists and returns a string.)

After writing the file, run `npx vitest run tests/connections/error-classifier.test.ts` and confirm the suite is **red** (it must fail because `error-classifier.ts` does not exist — that is the desired Wave 0 state).
  </action>
  <verify>
    <automated>npx vitest run tests/connections/error-classifier.test.ts 2>&1 | grep -E "Cannot find module|FAIL|error-classifier" | head -5</automated>
  </verify>
  <done>File `tests/connections/error-classifier.test.ts` exists. `npx vitest run tests/connections/error-classifier.test.ts -t "classifies fatal sources"`, `-t "classifies transient sources"`, and `-t "defaults unknown to transient"` all match a test (red because module missing). File compiles with `tsc --noEmit` (the missing-module error is at runtime, not type-check, because Vitest uses ts-jit).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend tests/connections/account-connection.test.ts with red CONN-02..CONN-06 scaffolds</name>
  <files>tests/connections/account-connection.test.ts</files>
  <read_first>
    - tests/connections/account-connection.test.ts (existing file — extend, do not rewrite; preserve `vi.mock("imapflow", ...)`, `makeMockClient`, `flushMicrotasks`)
    - .planning/phases/12-connection-resilience-foundation/12-VALIDATION.md (the `-t` names for the new tests MUST match this table exactly)
    - .planning/phases/12-connection-resilience-foundation/12-CONTEXT.md (D-02, D-08, D-09, D-10, D-11, D-13)
    - .planning/phases/12-connection-resilience-foundation/12-RESEARCH.md §"Pattern 3", §"Code Examples Example 8" (existing test pattern for race / listener-leak / fake timer drive)
    - src/connections/account-connection.ts (current shape — note `BACKOFF_MAX_ATTEMPTS = 10` will go away)
  </read_first>
  <action>
APPEND new red tests to the existing `tests/connections/account-connection.test.ts`. Do NOT delete existing tests in this plan — Plan 03 will remove/replace obsolete ones (notably the 10-attempt-cap test). Reuse the existing `vi.mock("imapflow", ...)`, `makeMockClient`, `flushMicrotasks`, and `makeAccountConfig` helpers already in the file.

Add the following `it(...)` blocks, each with `-t` text matching 12-VALIDATION.md verbatim. Place them inside the existing `describe("AccountConnection state machine", ...)` block (extend it) or inside new `describe` blocks — either is fine as long as the `-t` filter matches.

1. `it("unbounded transient retry survives 15 consecutive transient failures and eventually connects", async () => { ... })`
   - Mock `ImapFlow` constructor so that the first 15 returned mock clients have `connect: vi.fn().mockRejectedValue(new Error("ECONNRESET test"))` and the 16th resolves
   - Tip: use `MockImapFlow.mockImplementation` and a counter, OR call `.mockImplementationOnce` 15 times then `.mockImplementation` for the success path
   - Call `await conn.connect()` (initial connect throws → reconnect loop runs)
   - Loop the test: `for (let i = 0; i < 16; i++) { await flushMicrotasks(); await vi.advanceTimersByTimeAsync(150_000); }` (drive past the 120_000ms backoff cap)
   - Assert `conn.getStatus().kind === "connected"`
   - This test will be red until Plan 03 removes `BACKOFF_MAX_ATTEMPTS` and switches the loop to `while (!isShuttingDown)`.

2. `it("full-jitter backoff produces values in [0, capped) range with mocked Math.random", async () => { ... })`
   - Spy on `Math.random` returning `0.5`; drive the loop once; assert the sleep timer was set with a value between 0 and `1000` for attempt 1 (`Math.floor(0.5 * 1000) === 500`)
   - Spy returning `0.999`; same assertion gives `999`
   - Use `vi.spyOn(Math, "random").mockReturnValue(0.5)` and `expect(globalThis.setTimeout).toHaveBeenCalledWith(expect.any(Function), 500)` (with `vi.spyOn(globalThis, "setTimeout")` BEFORE the connect call). Note: the existing `sleep` uses `globalThis.setTimeout`, which is spy-able.
   - Red because current code uses `Math.min(raw, BACKOFF_CAP_MS)` (no jitter).

3. `it("fatal goes straight to suspended on attempt 1 with no further retries", async () => { ... })`
   - Mock ImapFlow constructor so `connect` always rejects with `Object.assign(new Error("auth failed"), { serverResponseCode: "AUTHENTICATIONFAILED" })` (D-05 RFC 5530 fatal code — avoids requiring the `AuthenticationFailure` class shape)
   - `await conn.connect()`; `await flushMicrotasks(10)`; `await vi.advanceTimersByTimeAsync(2_000)` (in case any initial backoff slipped through — but per RESEARCH "Initial-connect fatal fast-path" there should be ZERO sleep)
   - Assert `conn.getStatus().kind === "suspended"` and `conn.getStatus().reason` contains the word "Authentication" or "authentication" (case-insensitive — Plan 03 locks D-02 stock strings)
   - Assert `vi.mocked(ImapFlow).mock.calls.length === 1` (only the initial-connect client; no second client built)
   - Red because current code uses 4-state union without `suspended` and would loop 10 times.

4. `it("buildClient applies TCP keepalive: socketOptions.keepAlive true, keepAliveInitialDelay 60000, socketTimeout 90000", async () => { ... })`
   - `await conn.connect()`
   - Assert: `expect(vi.mocked(ImapFlow)).toHaveBeenCalledWith(expect.objectContaining({ socketOptions: { keepAlive: true, keepAliveInitialDelay: 60_000 }, socketTimeout: 90_000 }));`
   - Red because current `buildClient` sets `socketTimeout: 300_000` and no `socketOptions`.

5. `it("concurrent close events trigger exactly one reconnect loop (one new ImapFlow constructed)", async () => { ... })`
   - `await conn.connect()` → state is `connected`
   - `const callsBefore = vi.mocked(ImapFlow).mock.calls.length;`
   - Get the connected client; emit `close` twice synchronously: `status.client.emit("close"); status.client.emit("close");`
   - `await flushMicrotasks(10); await vi.advanceTimersByTimeAsync(2_000);` (past first backoff window for jittered delay up to 1s)
   - Assert `vi.mocked(ImapFlow).mock.calls.length === callsBefore + 1` (exactly ONE new client)
   - Red because current `status.kind === "reconnecting"` guard races (the async status mutation has not happened yet when the second `close` fires).

6. `it("listener cleanup: removeAllListeners is invoked on every discarded client across 5 reconnect failures", async () => { ... })`
   - Create a counter of `removeAllListeners` invocations: replace `makeMockClient` for this test (or instrument it) so each mock client tracks `removeAllListenersCalled: number`
   - Force 5 transient connect failures then one success
   - After the loop completes, assert at least 5 of the discarded mock clients had `removeAllListeners` called exactly once
   - Suggested implementation: keep a module-level `const builtClients: Array<{ removeAllListeners: ReturnType<typeof vi.fn> }> = []`; in `MockImapFlow`, push the new client to the array; spy on `removeAllListeners`
   - Red because current code never calls `removeAllListeners`.

After writing, run `npx vitest run tests/connections/account-connection.test.ts` and confirm new tests are red while EXISTING tests still pass (the only existing tests this plan doesn't touch should still be green — the 10-attempt-cap test will become red in Plan 03 when we delete that constant; for now it stays green).
  </action>
  <verify>
    <automated>npx vitest run tests/connections/account-connection.test.ts -t "unbounded transient retry" -t "full-jitter backoff" -t "fatal goes straight to suspended" -t "buildClient applies TCP keepalive" -t "concurrent close events" -t "listener cleanup" 2>&1 | tail -20</automated>
  </verify>
  <done>All six new `it(...)` blocks exist with `-t` strings exactly matching 12-VALIDATION.md. Running the file shows the six new tests RED. Existing tests still pass (or at minimum, no NEW failures appear beyond the six scaffolds).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend connection-manager / poller / startup tests with remaining red scaffolds</name>
  <files>tests/connections/connection-manager.test.ts, tests/polling/poller.test.ts, tests/startup.test.ts</files>
  <read_first>
    - tests/connections/connection-manager.test.ts (existing — extend with `suspended` case)
    - tests/polling/poller.test.ts (existing — extend with skip tests)
    - tests/startup.test.ts (existing — extend with unhandledRejection test)
    - .planning/phases/12-connection-resilience-foundation/12-VALIDATION.md (exact `-t` text)
    - .planning/phases/12-connection-resilience-foundation/12-CONTEXT.md (D-12 unhandledRejection, D-15 poller skip)
    - src/connections/connection-manager.ts (`getClient` switch — currently has `failed` case)
    - src/polling/poller.ts (`pollAccount` — currently throws on getClient error)
    - src/index.ts (no `process.on('unhandledRejection', ...)` handler yet)
  </read_first>
  <action>
Three separate test files to extend. Append new `it(...)` blocks; do NOT delete the existing `failed` case tests in `connection-manager.test.ts` — Plan 04 will update them when the state union changes.

**File 1: `tests/connections/connection-manager.test.ts`**

Add one new `describe` block (or extend the existing one) with:

`it("getClient returns structured error string when account is suspended", async () => { ... })`
- The test uses the `vi.mock("imapflow", ...)` pattern. Since `ConnectionManager` only exposes `getStatus`/`getClient`, you need a way to force the underlying `AccountConnection` into the `suspended` state. Cleanest path: mock the ImapFlow `connect` to reject with `Object.assign(new Error("auth"), { serverResponseCode: "AUTHENTICATIONFAILED" })`, call `await manager.connectAll()`, then `await flushMicrotasks(10)`. After Plan 03 ships, the account will be in `{ kind: "suspended", reason, since }`.
- Assert: `const result = manager.getClient("personal"); expect("error" in result).toBe(true); if ("error" in result) expect(result.error).toMatch(/suspended/i);`
- Red because (a) the state machine has no `suspended` variant yet and (b) `getClient`'s switch has no `suspended` case.

**File 2: `tests/polling/poller.test.ts`**

Append two new `it(...)` blocks to whatever describe block hosts Poller tests:

a. `it("skips non-connected accounts: no IMAP call when status is reconnecting/suspended/connecting", async () => { ... })`
   - Build a `ConnectionManager` mock with `getStatus(accountId)` returning `{ kind: "reconnecting", attempt: 3, nextRetryAt: new Date(), lastError: "ECONNRESET" }` for one account and `{ kind: "connected", client: mockImapFlow }` for another
   - Construct `new Poller(managerMock, 1)`; call `await (poller as unknown as { poll: () => Promise<void> }).poll()` (use the private accessor pattern existing tests use) or invoke via `poller.start()` + advance timers
   - Spy on `searchMessages` (`vi.mock("../../src/services/search-service.js", () => ({ searchMessages: vi.fn().mockResolvedValue([]) }))`); assert `searchMessages` was called ONCE (for the connected account only)
   - Spy on `logger.debug` (`vi.mock("../../src/logger.js", ...)`) and assert exactly one `debug` call mentioning the reconnecting accountId per poll cycle
   - Spy on `logger.error` and assert it was NOT called for the skipped account (current behavior throws → `logger.error("failed to poll account ...")`)
   - Red because current `pollAccount` calls `getClient` and throws when status is non-connected.

b. `it("skip is not sticky — skipped accounts are re-checked on the next poll cycle", async () => { ... })`
   - Same setup as (a), but flip the reconnecting account's status to `{ kind: "connected", client: mockImapFlow }` between cycles
   - Drive two poll cycles (use `vi.advanceTimersByTimeAsync(intervalSeconds * 1000 + 100)` between)
   - Assert `searchMessages` was called once per cycle for the now-connected account (so total = 1 from cycle 1 + 2 from cycle 2 = 3, or however the existing test structure tallies)
   - Assert the `skipLoggedThisCycle`-style behavior: debug log appears in cycle 1 (skipped), NOT in cycle 2 (recovered)
   - Red because the skip mechanism does not exist yet.

If the existing poller tests do not provide a `ConnectionManager` mock factory, create a minimal inline one in the test:
```typescript
const makeManagerMock = (statuses: Record<string, AccountConnectionStatus | { error: string }>) => ({
  getAccountIds: () => Object.keys(statuses),
  getStatus: (id: string) => statuses[id],
  getClient: (id: string) => "error" in statuses[id] ? statuses[id] : (statuses[id] as { client: ImapFlow }).client,
} as unknown as ConnectionManager);
```

**File 3: `tests/startup.test.ts`**

Append a new `describe("unhandledRejection handler", () => { ... })` block:

`it("unhandledRejection logs and continues", async () => { ... })`
- Spy on `logger.error` (or whatever the project exposes — confirm by reading `src/logger.ts` if unclear; for now use `vi.spyOn(logger, "error")`)
- Spy on `process.exit` — `const exitSpy = vi.spyOn(process, "exit").mockImplementation(((_code?: number) => undefined) as never);`
- Trigger an unhandled rejection: `process.emit("unhandledRejection", new Error("test rejection"), Promise.reject(new Error("test rejection")).catch(() => {}));`
- Assert `logger.error` was called with a message containing `"unhandledRejection"` (or whatever phrase Plan 04 chooses — settle on the phrase "unhandledRejection" since Pattern 6 in RESEARCH.md uses it)
- Assert `exitSpy` was NOT called
- The test will need to import `main` from `src/index.ts` OR import a helper that registers the handler. Cleanest path: Plan 04 will export an `installUnhandledRejectionHandler(logger)` function from `src/index.ts` (or a new `src/process-handlers.ts`) — the test imports that function and calls it.
- Until Plan 04 ships, the import will fail → test is red. That's the desired Wave 0 state.

After writing all three files, run `npm test` and confirm the new tests are red while the rest of the suite remains green.
  </action>
  <verify>
    <automated>npx vitest run tests/connections/connection-manager.test.ts tests/polling/poller.test.ts tests/startup.test.ts -t "suspended" -t "skips non-connected accounts" -t "skip is not sticky" -t "unhandledRejection logs and continues" 2>&1 | tail -15</automated>
  </verify>
  <done>Four new red tests exist with `-t` text matching 12-VALIDATION.md exactly. `npm test` shows the four new failures alongside any pre-existing failures triggered by the suite expansion (e.g., manager `suspended` test may chain-fail with account-connection tests — that is acceptable as long as the scaffolds themselves match the validation table).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | No boundary crossed — Wave 0 only touches `tests/`. |
| package install | None this plan — no `npm install` invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | test scaffolds drift from VALIDATION.md `-t` names | mitigate | Each task's `<acceptance_criteria>` runs `vitest -t "<exact-string>"` and grep-matches the title — drift surfaces immediately. |
| T-12-02 | Information Disclosure | test asserts on full `err.message` including credentials in `humanReason` | mitigate | Task 1's `humanReason` smoke test uses `toMatch(/authentication/i)` instead of an exact-string compare; Plan 02 locks the stock-string set per D-02. |
</threat_model>

<verification>
- All Wave 0 test files exist and compile (or fail at runtime via missing-module, not at TypeScript-check time)
- Every `-t` name in 12-VALIDATION.md matches at least one `it(...)` title
- `npm test` exit code is non-zero (suite is red — expected) AND no test file is silently skipped (`vitest --reporter=verbose` lists each new test)
- No production file under `src/` is modified by this plan (grep `git status` to confirm)
</verification>

<success_criteria>
- `npx vitest run tests/connections/error-classifier.test.ts` reports the file as failing-to-load (module missing) or red — that is the goal state
- `npx vitest run tests/connections/account-connection.test.ts` shows the six new tests red; existing passing tests still pass
- `npx vitest run tests/polling/poller.test.ts -t "skips non-connected accounts"` and `-t "skip is not sticky"` match exactly one test each
- `npx vitest run tests/startup.test.ts -t "unhandledRejection logs and continues"` matches exactly one test
- `git diff --name-only src/` shows zero output (no production changes)
</success_criteria>

<output>
Create `.planning/phases/12-connection-resilience-foundation/12-01-SUMMARY.md` when done, summarizing which `-t` strings were added per file and confirming the red state.
</output>
