---
phase: 13-health-surface-cache-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/connections/account-connection.ts
  - src/connections/connection-manager.ts
  - tests/connections/account-connection.test.ts
  - tests/connections/connection-manager.test.ts
autonomous: true
requirements:
  - HEALTH-01
  - HEALTH-02
  - HEALTH-03
requirements_addressed:
  - HEALTH-01
  - HEALTH-02
  - HEALTH-03

must_haves:
  truths:
    - "`AccountConnection` exposes `connectedAt`, `lastError`, and `lastErrorAt` via public accessors (currently they are private fields with no read path)"
    - "`AccountConnection.lastErrorAt` is stamped to a fresh `Date` at every site where `lastError` is set to a string (D-07)"
    - "`AccountConnection.lastErrorAt` is cleared to `null` at every site where `lastError` is cleared to `null` (D-07)"
    - "`ConnectionManager` delegates `getLastConnectedAt(id)`, `getLastError(id)`, `getLastErrorAt(id)` to the matching `AccountConnection` instance and returns `null` for unknown accounts"
    - "Existing Phase 12 tests (state machine, reconnect loop, suspended fast-path) remain green — no behavioral regression"
  artifacts:
    - path: "src/connections/account-connection.ts"
      provides: "Private `lastErrorAt: Date | null` field + three accessors `getConnectedAt()`, `getLastError()`, `getLastErrorAt()`"
      contains: "private lastErrorAt: Date | null = null"
    - path: "src/connections/connection-manager.ts"
      provides: "Three delegating accessors `getLastConnectedAt(id)`, `getLastError(id)`, `getLastErrorAt(id)`"
      contains: "getLastConnectedAt(accountId: string): Date | null"
    - path: "tests/connections/account-connection.test.ts"
      provides: "RED-then-GREEN tests for lastErrorAt stamp/clear semantics + accessor wiring"
      contains: 'describe("HEALTH-02: lastErrorAt stamp + clear"'
    - path: "tests/connections/connection-manager.test.ts"
      provides: "Delegation tests for the three new manager accessors"
      contains: 'describe("HEALTH-02: ConnectionManager health accessors"'
  key_links:
    - from: "src/connections/connection-manager.ts"
      to: "src/connections/account-connection.ts"
      via: "this.connections.get(id)?.getLastErrorAt() ?? null"
      pattern: "getLast(ConnectedAt|Error|ErrorAt)\\(accountId"
    - from: "src/connections/account-connection.ts (reconnect loop catch)"
      to: "src/connections/account-connection.ts (this.lastErrorAt = new Date())"
      via: "Every `this.lastError = message` write is paired with `this.lastErrorAt = new Date()`"
      pattern: "this\\.lastErrorAt = new Date\\(\\)"
---

<objective>
Internal health-field foundation for Phase 13. Adds the `lastErrorAt: Date | null` field to `AccountConnection` (D-07), adds three public accessors on `AccountConnection` (`getConnectedAt`, `getLastError`, `getLastErrorAt`), and adds three matching delegating accessors on `ConnectionManager` (`getLastConnectedAt`, `getLastError`, `getLastErrorAt`). No tool-layer changes — that is Plan 02.

Purpose: Plan 02's `list_accounts` switch (HEALTH-02 / HEALTH-03) needs a stable read path to per-account `connectedAt` / `lastError` / `lastErrorAt`. Phase 12 added the first two as private fields specifically for Phase 13 to surface; this plan adds the third field and unlocks all three via accessors. Surfacing `lastError`/`lastErrorAt` here is V5 ASVS-relevant: this plan exposes raw-message values internally, but the tool layer (Plan 02) is forbidden from surfacing them directly for the `reconnecting` branch — see Plan 02 for the sanitization contract.

Output: `lastErrorAt` field on `AccountConnection`, six new accessors (3 on each class), green tests proving the stamp/clear pairing and delegation behavior.
</objective>

<execution_context>
@/Users/jonathanhowell/code/imap-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathanhowell/code/imap-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-health-surface-cache-improvements/13-CONTEXT.md
@.planning/phases/13-health-surface-cache-improvements/13-RESEARCH.md
@.planning/phases/13-health-surface-cache-improvements/13-VALIDATION.md

# Read these before touching the modified files
@src/connections/account-connection.ts
@src/connections/connection-manager.ts
@tests/connections/account-connection.test.ts
@tests/connections/connection-manager.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Already in the codebase. -->

From src/connections/account-connection.ts:
- export type AccountConnectionStatus = { kind: "connecting" } | { kind: "connected"; client: ImapFlow } | { kind: "reconnecting"; attempt: number; nextRetryAt: Date; lastError: string } | { kind: "suspended"; reason: string; since: Date }
- Private fields ALREADY present (Phase 12 groundwork, lines 99-100):
  - private connectedAt: Date | null = null
  - private lastError: string | null = null
- Phase 13 adds: private lastErrorAt: Date | null = null
- Stamp/clear sites currently touch this.lastError:
  - line 229: this.lastError = null   (successful reconnect)
  - line 234: this.lastError = message (transient failure in reconnect loop)
  - line 297: this.lastError = null   (successful initial connect)
  - line 301: this.lastError = message (initial connect failure)

From src/connections/connection-manager.ts:
- Pattern for getStatus(accountId) to follow (lines 76-82): looks up the AccountConnection by id and either returns { error: string } for an unknown id OR delegates to connection.getStatus().
- Phase 13 health accessors DEVIATE: return null (not { error }) for unknown accounts — health fields are Date | null / string | null by design.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Failing tests for AccountConnection lastErrorAt field + accessors</name>
  <files>tests/connections/account-connection.test.ts</files>
  <read_first>
    - tests/connections/account-connection.test.ts (lines 1-200, understand existing `vi.mock("imapflow")` setup and mock helpers `makeMockClient`, `flushMicrotasks`, `makeAccountConfig`)
    - src/connections/account-connection.ts (lines 83-110, 188-275, 285-328 — current private field declarations, reconnect loop catch with `this.lastError = message`, and initial-connect catch)
    - .planning/phases/13-health-surface-cache-improvements/13-CONTEXT.md (D-07 exact wording)
    - .planning/phases/13-health-surface-cache-improvements/13-RESEARCH.md (sections "Code Surface Inventory" → "AccountConnection — Health Fields" and "Validation Architecture" → "Wave 0 Gaps")
  </read_first>
  <behavior>
    Failing tests prove the contract before implementation. Add a new `describe("HEALTH-02: lastErrorAt stamp + clear")` block at the bottom of `tests/connections/account-connection.test.ts` with the following `it` blocks:

    - Test A: `getConnectedAt() returns null before connect, returns a Date after successful connect()` — instantiate `AccountConnection`, assert `conn.getConnectedAt() === null`, call `await conn.connect()`, assert `conn.getConnectedAt() instanceof Date`.
    - Test B: `getLastError() returns null on successful connect (clears any previous value)` — instantiate, await connect, assert `conn.getLastError() === null`.
    - Test C: `getLastErrorAt() returns null on successful connect (clears any previous value)` — instantiate, await connect, assert `conn.getLastErrorAt() === null`.
    - Test D: `getLastError() returns the error message AND getLastErrorAt() returns a Date after initial-connect transient failure` — mock `client.connect` to reject with `new Error("ECONNRESET")` on the FIRST attempt and resolve thereafter (so the reconnect loop succeeds on attempt 2 and `connect()` returns). Use the existing `vi.useFakeTimers()` pattern and advance time past the first backoff sleep to let the reconnect loop attempt. After the failure has been registered but before the next attempt succeeds (e.g. inspect state mid-loop using `vi.advanceTimersByTimeAsync(0)` then probe getLastError/getLastErrorAt), confirm `conn.getLastError()` is the string "ECONNRESET" and `conn.getLastErrorAt() instanceof Date`. NOTE: the existing test file already uses `vi.useFakeTimers()` in `beforeEach`, follow the same pattern.
    - Test E: `lastErrorAt is stamped to a fresh Date on reconnect-loop transient failure` — connect successfully, emit `close` to enter reconnect loop, have the next `client.connect` mock reject with `new Error("ETIMEDOUT")`. Use `vi.advanceTimersByTimeAsync` to step through the backoff sleep. After the failed reconnect attempt, assert `conn.getLastErrorAt() instanceof Date`. The exact wall-clock time is not asserted (jitter); the instance type is the contract.
    - Test F: `lastErrorAt is cleared to null on successful reconnect` — same setup as E but the SECOND reconnect attempt succeeds. After success, assert both `conn.getLastError() === null` AND `conn.getLastErrorAt() === null`.

    All tests use the existing `makeMockClient` / `makeAccountConfig` helpers. The `vi.mock("imapflow", ...)` factory at the top of the file already covers the `AuthenticationFailure` stub — do not modify it.

    These tests MUST fail BEFORE Task 2 (RED), then pass AFTER Task 2 (GREEN). If they pass before Task 2 the test is invalid — Task 2 has nothing to prove.
  </behavior>
  <action>
    Append a new `describe("HEALTH-02: lastErrorAt stamp + clear", () =&gt; { ... })` block to `tests/connections/account-connection.test.ts` containing the six tests A through F described in `<behavior>`. Use the existing `vi.useFakeTimers()` / `flushMicrotasks` patterns from the file. Do not modify any existing tests. Do not modify the existing `vi.mock("imapflow", ...)` factory. Per CONTEXT.md D-07, the new field name is `lastErrorAt` (camelCase internal) and the accessor names are `getConnectedAt`, `getLastError`, `getLastErrorAt` — note: NO snake_case here. Snake_case only applies at the MCP tool response boundary in Plan 02. Run the file and verify the 6 new tests FAIL (RED). Existing tests must remain GREEN.
  </action>
  <verify>
    <automated>npm test -- tests/connections/account-connection.test.ts 2>&amp;1 | tee /tmp/13-01-task1.log; grep -q 'HEALTH-02: lastErrorAt stamp + clear' tests/connections/account-connection.test.ts &amp;&amp; echo "describe-present-OK"; grep -E '(FAIL|×|fail).*HEALTH-02' /tmp/13-01-task1.log || true</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal string `describe("HEALTH-02: lastErrorAt stamp + clear"` (grep-verifiable: `grep -c 'HEALTH-02: lastErrorAt stamp + clear' tests/connections/account-connection.test.ts` returns 1)
    - File contains 6 new `it(` invocations inside that describe block
    - All 6 new tests fail at this stage with messages indicating either "is not a function" (accessor missing) or expected-null vs unstamped-value
    - All pre-existing tests in the file still pass
    - `npm test -- tests/connections/account-connection.test.ts` exits non-zero (test suite failure expected at RED stage)
  </acceptance_criteria>
  <done>6 new HEALTH-02 tests exist in `account-connection.test.ts` and fail because the field + accessors do not exist yet. No existing tests regressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Add lastErrorAt field + 3 accessors on AccountConnection</name>
  <files>src/connections/account-connection.ts</files>
  <read_first>
    - src/connections/account-connection.ts (full file — lines 83-351)
    - tests/connections/account-connection.test.ts (the new HEALTH-02 describe block written in Task 1 — proves the contract)
    - .planning/phases/13-health-surface-cache-improvements/13-CONTEXT.md (D-07 verbatim)
    - .planning/phases/13-health-surface-cache-improvements/13-RESEARCH.md (Pitfall 1 — explicitly note that `getLastError()` returns the raw `err.message` field; Plan 02 is the gate that prevents this from reaching agents for the reconnecting branch)
  </read_first>
  <action>
    Make the Task 1 RED tests green. Concrete changes to `src/connections/account-connection.ts`:

    1. Add a new private field immediately after line 100 (`private lastError: string | null = null;`):
       `private lastErrorAt: Date | null = null;`
       Match the existing two-field comment block style (lines 96-100). Extend the existing Phase-13-groundwork comment to mention the new field as well.

    2. Add three public accessor methods next to the existing `getStatus()` method (line 110-112). Keep them as a contiguous block right after `getStatus()`:
       - `getConnectedAt(): Date | null { return this.connectedAt; }`
       - `getLastError(): string | null { return this.lastError; }`
       - `getLastErrorAt(): Date | null { return this.lastErrorAt; }`

       Add a JSDoc above the trio that reads exactly (in JSDoc syntax):
       "Phase 13 (HEALTH-02 / HEALTH-03): public read path to the internal health fields. SECURITY (T-12-09 / V5 ASVS): getLastError() returns the raw err.message — Plan 13-02 (list_accounts) MUST NOT surface this directly for the reconnecting branch. Plan 13-02 either substitutes a stock template or returns last_error: null for reconnecting accounts."

    3. Pair `lastErrorAt` writes to every `lastError` write site. Exact edits (line numbers refer to the current file — re-grep before editing in case the file has shifted by a line or two from prior edits):

       - At line 229 (`this.lastError = null;` — successful reconnect): add `this.lastErrorAt = null;` on the next line.
       - At line 234 (`this.lastError = message;` — reconnect transient failure): add `this.lastErrorAt = new Date();` on the next line.
       - At line 297 (`this.lastError = null;` — successful initial connect): add `this.lastErrorAt = null;` on the next line.
       - At line 301 (`this.lastError = message;` — initial connect failure): add `this.lastErrorAt = new Date();` on the next line.

       Confirm the four sites before editing — `grep -n "this\\.lastError = " src/connections/account-connection.ts` should return exactly those 4 lines. Each must get a paired `this.lastErrorAt = ...` on the following line.

    4. NO changes to `runReconnectLoop`, `connect`, `gracefulClose`, `buildClient`, `wireListeners`, or the `AccountConnectionStatus` union itself. The union is sealed by Phase 12 — Phase 13 does not modify it.

    Run the file's tests and confirm all pass (existing + 6 new from Task 1).
  </action>
  <verify>
    <automated>npm test -- tests/connections/account-connection.test.ts &amp;&amp; test "$(grep -c 'this\.lastErrorAt = ' src/connections/account-connection.ts)" = "4" &amp;&amp; grep -q 'private lastErrorAt: Date | null = null;' src/connections/account-connection.ts &amp;&amp; test "$(grep -cE '^\s+get(ConnectedAt|LastError|LastErrorAt)\(\)' src/connections/account-connection.ts)" = "3"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'this\.lastErrorAt = ' src/connections/account-connection.ts` returns exactly 4 (matches the 4 stamp/clear sites)
    - `grep -c 'private lastErrorAt: Date | null = null;' src/connections/account-connection.ts` returns 1
    - `grep -cE '^\s+get(ConnectedAt|LastError|LastErrorAt)\(\)' src/connections/account-connection.ts` returns 3
    - `npm test -- tests/connections/account-connection.test.ts` exits zero — all tests pass (existing Phase 12 tests + 6 new HEALTH-02 tests)
    - No changes to the `AccountConnectionStatus` type union (verify by `grep -c 'export type AccountConnectionStatus' src/connections/account-connection.ts` returning 1 and the union still has exactly 4 variants)
  </acceptance_criteria>
  <done>The 6 RED tests from Task 1 are GREEN. Accessors expose the three health fields. lastErrorAt is stamped/cleared in lockstep with lastError at all four sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConnectionManager delegating accessors + RED-then-GREEN tests</name>
  <files>tests/connections/connection-manager.test.ts, src/connections/connection-manager.ts</files>
  <read_first>
    - tests/connections/connection-manager.test.ts (lines 1-120 — understand existing `vi.mock("imapflow")` factory and `makeTwoAccountConfig` helper)
    - src/connections/connection-manager.ts (full file — lines 1-113 — note `getStatus(accountId)` pattern at lines 76-82 which Phase 13 accessors mirror but with different unknown-account behavior)
    - src/connections/account-connection.ts (the 3 new accessors from Task 2 — these are the delegation targets)
    - .planning/phases/13-health-surface-cache-improvements/13-CONTEXT.md (D-07 and the canonical-refs entry mentioning ConnectionManager)
  </read_first>
  <behavior>
    Combined RED+GREEN step (single task because the test file and source file are tightly coupled: the test mocks the manager, calls accessors, and asserts delegation). Add a new `describe("HEALTH-02: ConnectionManager health accessors")` block to `tests/connections/connection-manager.test.ts` with the following `it` blocks, then implement the manager accessors so they pass:

    - Test A: `getLastConnectedAt returns the AccountConnection.getConnectedAt value for a known account` — construct a `ConnectionManager` from a 2-account config, call `connectAll()`, then assert `manager.getLastConnectedAt("personal") instanceof Date`.
    - Test B: `getLastConnectedAt returns null for an unknown account` — assert `manager.getLastConnectedAt("nonexistent") === null` (NOT `{ error }`, unlike `getStatus()`).
    - Test C: `getLastError returns null for a freshly-connected account` — after `connectAll()`, assert `manager.getLastError("personal") === null`.
    - Test D: `getLastError returns null for an unknown account`.
    - Test E: `getLastErrorAt returns null for a freshly-connected account`.
    - Test F: `getLastErrorAt returns null for an unknown account`.

    Implementation pattern matches `getStatus()` (lines 76-82) but returns `null` for unknown accounts instead of `{ error: string }`.
  </behavior>
  <action>
    Step 3a: Append the new `describe("HEALTH-02: ConnectionManager health accessors", () =&gt; { ... })` block to `tests/connections/connection-manager.test.ts` containing the 6 tests A through F. Run `npm test -- tests/connections/connection-manager.test.ts`. RED expected: the 6 new tests fail because `getLastConnectedAt` / `getLastError` / `getLastErrorAt` do not exist on `ConnectionManager` yet.

    Step 3b: Add three methods to `src/connections/connection-manager.ts` (immediately after the existing `getStatus(accountId)` method at line 82, before `getAccountIds()`):

    ```
    getLastConnectedAt(accountId: string): Date | null {
      return this.connections.get(accountId)?.getConnectedAt() ?? null;
    }

    getLastError(accountId: string): string | null {
      return this.connections.get(accountId)?.getLastError() ?? null;
    }

    getLastErrorAt(accountId: string): Date | null {
      return this.connections.get(accountId)?.getLastErrorAt() ?? null;
    }
    ```

    Add a JSDoc above the three new methods:
    "Phase 13 (HEALTH-02 / HEALTH-03): delegating health accessors. Unknown accounts return null (not { error }) because health fields are Date | null / string | null by design — the tool layer treats null as 'no value' uniformly. SECURITY: getLastError exposes the raw err.message; consumers (Plan 13-02 handleListAccounts) must sanitize for the reconnecting branch per T-12-09 / V5 ASVS."

    Run the test file again. GREEN expected.
  </action>
  <verify>
    <automated>npm test -- tests/connections/connection-manager.test.ts &amp;&amp; grep -q 'HEALTH-02: ConnectionManager health accessors' tests/connections/connection-manager.test.ts &amp;&amp; grep -q 'getLastConnectedAt(accountId: string): Date | null' src/connections/connection-manager.ts &amp;&amp; grep -q 'getLastError(accountId: string): string | null' src/connections/connection-manager.ts &amp;&amp; grep -q 'getLastErrorAt(accountId: string): Date | null' src/connections/connection-manager.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'HEALTH-02: ConnectionManager health accessors' tests/connections/connection-manager.test.ts` returns 1
    - Test file contains 6 new `it(` cases inside that describe block
    - `src/connections/connection-manager.ts` contains the three method signatures verbatim (grep-verifiable):
      - `getLastConnectedAt(accountId: string): Date | null`
      - `getLastError(accountId: string): string | null`
      - `getLastErrorAt(accountId: string): Date | null`
    - Each method body uses the pattern `this.connections.get(accountId)?.getX() ?? null` (grep `grep -c 'this.connections.get(accountId)?.get' src/connections/connection-manager.ts` returns at least 3)
    - `npm test -- tests/connections/connection-manager.test.ts` exits zero
    - `npm test` (full suite) exits zero — no regressions to Phase 12 tests
  </acceptance_criteria>
  <done>ConnectionManager exposes three delegating accessors. All 6 new tests pass. Full suite stays at 246+ passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccountConnection internal → ConnectionManager → tool layer | Untrusted `err.message` text (potentially containing `auth.user`) propagates from imapflow through `this.lastError` (raw) into `manager.getLastError()` (still raw). The tool layer is the LAST sanitization gate before agent output. Plan 13-02 must NOT pass raw `lastError` through to `last_error` for the reconnecting branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | `AccountConnection.getLastError()` returns raw `err.message` which imapflow may populate with `auth.user` (username) text per Phase 12 T-12-09 analysis | mitigate-downstream | This plan INTENTIONALLY exposes the raw value as an internal accessor — `handleListAccounts` (Plan 13-02) is required to sanitize or null-out the field for the `reconnecting` branch. Plan 13-02 enforces this with a stock-string substitution and a test asserting `last_error` does not contain ECONNRESET / IMAP error text. Documented in the JSDoc on `getLastError()` (Task 2 step 2). |
| T-13-02 | Tampering | None — no untrusted writes; the new field is set only by the same code paths that set `lastError` | accept | Phase 12 already audits the four write sites; Phase 13 mirrors them. |
| T-13-SC | Tampering | npm installs | n/a | Zero packages installed in this plan (verified: no `npm install` in any task action). |
</threat_model>

<verification>
- `npm test -- tests/connections/account-connection.test.ts` exits zero
- `npm test -- tests/connections/connection-manager.test.ts` exits zero
- `npm test` (full suite) exits zero, ≥252 passing (246 baseline + 12 new tests: 6 + 6)
- `npx tsc --noEmit` exits zero — no new type errors
- `grep -c 'this\.lastErrorAt = ' src/connections/account-connection.ts` returns 4
- `grep -cE 'getLast(ConnectedAt|Error|ErrorAt)\(accountId' src/connections/connection-manager.ts` returns at least 3
</verification>

<success_criteria>
Plan 13-01 succeeds when:
1. `AccountConnection` has a `lastErrorAt: Date | null` field stamped/cleared at all 4 sites paired with `lastError`
2. `AccountConnection` exposes `getConnectedAt()`, `getLastError()`, `getLastErrorAt()` as public accessors
3. `ConnectionManager` exposes `getLastConnectedAt(id)`, `getLastError(id)`, `getLastErrorAt(id)` as delegating accessors returning `null` for unknown accounts
4. 12 new tests (6 on AccountConnection + 6 on ConnectionManager) prove stamp/clear pairing and delegation behavior
5. Full suite passes — Phase 12 regressions = zero
6. JSDoc on both accessor sets documents the T-12-09 sanitization contract that Plan 13-02 must enforce
</success_criteria>

<output>
Create `.planning/phases/13-health-surface-cache-improvements/13-01-SUMMARY.md` when done
</output>
